<h1>Leases</h1>

<% if @result.leases.empty? %>
  <p>No leases yet.</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Property</th>
        <th>Tenant</th>
        <th>Mobile</th>
        <th>Dates</th>
      </tr>
    </thead>
    <tbody>
      <% @result.leases.each do |l| %>
        <tr>
          <td><%= property_link(l.property_id) %></td>
          <td><%= link_to applicant_name(l.applicant_id), lease_path(l.id) %></td>
          <td>
            <% if (m = applicant_mobile(l.applicant_id)) %>
              <%= link_to m, "tel:#{m}" %>
            <% end %>
          </td>
          <td><%= link_to "#{l.start_date} → #{l.end_date || "open"}", lease_path(l.id) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
