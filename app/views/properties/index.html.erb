<h1>Properties</h1>

<p><%= link_to "Add property", new_property_path %></p>

<% if @result.properties.empty? %>
  <p>No properties yet.</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Address</th>
        <th>Status</th>
        <th>Beds</th>
        <th>Baths</th>
        <th>Public link</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @result.properties.each do |p| %>
        <tr>
          <td><%= link_to property_admin_label(p), property_path(p.slug) %></td>
          <td>
            <% if p.published %>
              <mark>Accepting applications</mark>
            <% else %>
              <small>Draft</small>
            <% end %>
          </td>
          <td><%= p.beds %></td>
          <td><%= p.baths %></td>
          <td><%= link_to p.slug, property_path(p.slug) %></td>
          <td>
            <%= button_to "Edit", edit_property_path(p.slug), method: :get, form: { style: "display:inline" } %>
            <% if p.published %>
              <%= button_to "Unpublish", unpublish_property_path(p.slug), method: :post, form: { style: "display:inline" } %>
            <% else %>
              <%= button_to "Publish", publish_property_path(p.slug), method: :post, form: { style: "display:inline" } %>
            <% end %>
            <%= button_to "Duplicate", duplicate_property_path(p.slug), method: :post, form: { style: "display:inline" } %>
            <%= button_to "Delete", property_path(p.slug), method: :delete, form: { style: "display:inline", data: { turbo_confirm: "Delete #{p.name}?" } } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
